# Runtime dependencies for the structured transfer study.
#
# Prefer `pip install -e .` (reads pyproject.toml) over this file -- it also
# installs the `structured_transfer` package so the scripts import cleanly.
# This file is kept for cluster environments where a plain requirements install
# is simpler.
#
# Install a torch build matching your CUDA version FIRST, e.g. for CUDA 12.4:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# The archived runs used torch 2.x + CUDA 12.4 on an NVIDIA A100-SXM-64GB.

# ── Core ─────────────────────────────────────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.23.0
pandas>=1.5.0
scikit-learn>=1.0.0
Pillow>=9.0.0
matplotlib>=3.6.0

# ── Optional; each degrades gracefully if absent ─────────────────────────────
# FLOP counting. Without it estimate_flops() falls back to a published constant
# and will NOT reproduce the paper's 4.11 G figure.
fvcore>=0.1.5
# Official WILDS metrics, used only by Evaluator.run_wilds_official().
wilds>=2.0.0
# WordNet, for deriving the TinyImageNet semantic class split. Without it,
# splits.py falls back to coarser keyword matching.
nltk>=3.8
# Fetching iWildCam metadata and streaming the mini image subset.
requests>=2.28.0

# ── Development ──────────────────────────────────────────────────────────────
pytest>=7.0.0
